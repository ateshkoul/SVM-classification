function [] = interpol_miss_value(A)
% function to replace missing values which were outliers in the data with
% interpolated values

% read the matrix


% find them missing values
is


% find the indices of the missing values


% interpolate

